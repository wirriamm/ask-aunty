<div data-controller='show-button'>
  <div class="d-flex">
    <div class="card shadow-sm mb-3 mr-1">
      <h4 class="p-3 m-0"><i class="fas fa-angle-left"></i></h4>
    </div>
    <div class="card shadow-sm mb-3 flex-grow-1">
      <h4 class="p-3 m-0">Makan Code: <%= @meal.vanity_id %></h4>
    </div>
  </div>

  <div>
    Jio your kakis through:
    <a href="<%= @whatsapp_link %>"><i class="fab fa-whatsapp"></i></a>
    <a href="<%= @telegram_link %>"><i class="fab fa-telegram-plane"></i></a>
  </div>

  <input type="hidden" data-target='show-button.pollCount' value="<%= Poll.where(user: current_user, meal: @meal).length %>" >
  <input type="hidden" data-target='show-button.time' value="<%= @meal.endtime > Time.now %>" >
  <%= link_to "Start the poll", setup_path(@meal.vanity_id), class: 'btn primarybtn', data: {target: "show-button.startPoll"} %>
  <%= link_to "View result", result_path(@meal.vanity_id), class: 'btn primarybtn', data: {target: "show-button.viewResult"} %>
</div>

<div>
  <h1>Instructions</h1>
  <p>This is a tinder app for cuisines</p>
  <p>Swipe right for the right cuisines</p>
  <p>What are not right... go to the left!</p>
</div>

